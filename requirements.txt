# تتبّع البث يعمل على مكتبة Python القياسية فقط.
# هذه المكتبات اختيارية لكنها موصى بها لتجزئة كلمات المرور بأمان (bcrypt).
# بدونها يسقط الكود تلقائياً إلى sha256.
passlib>=1.7.4,<2
bcrypt>=4.0.0,<6
